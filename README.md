# Modbud-NG
A cross-platform desktop GUI for connecting to, reading from, and writing to Modbus-enabled industrial control system (ICS) devices. Supports multiple simultaneous connections, per-session polling, and both Modbus TCP and Modbus RTU (serial) protocols. Compatible with Windows and Linux.
